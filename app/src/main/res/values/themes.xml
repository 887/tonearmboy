<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        D.17.2 — Tonearmboy runs the SplashScreen API as the launch theme.
        The system circle-clips the splash icon and we accept that — the
        round-cropped fox icon reads fine and earlier "make it square"
        attempts were not worth the moving parts. Activity body draws
        under Compose; the splash theme exists so the cold-boot frame the
        system shows before onCreate hands off cleanly to Compose without
        a white flash.

        - windowSplashScreenBackground: dark grey matching the launcher.
        - windowSplashScreenAnimatedIcon: launcher foreground (fox + vinyl).
        - windowSplashScreenIconBackgroundColor: matching the splash bg
          so Android sizes the icon at 160 dp instead of 80 dp.
        - postSplashScreenTheme: the regular Tonearmboy theme.
    -->
    <style name="Theme.Tonearmboy" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Tonearmboy.Main</item>
    </style>

    <style name="Theme.Tonearmboy.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launcher_background</item>
    </style>

</resources>
